{
  "name": "ggsipu-hostel-portal",
  "version": "1.0.0",
  "private": true,
  "description": "GGSIPU Hostel Allocation Portal — student-facing application, Vercel Serverless Functions + Google Sheets/Drive backend.",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "get-refresh-token": "node scripts/get-refresh-token.js",
    "check-hostel-gender-mismatches": "node scripts/check-hostel-gender-mismatches.js"
  },
  "dependencies": {
    "googleapis": "^144.0.0"
  },
  "devDependencies": {
    "dotenv": "^16.4.5"
  }
}
